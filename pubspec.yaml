name: report_manager
description: "Fuel Reporting Management"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  get_it: ^7.6.7
  gsheets: ^0.5.0
  provider: ^6.1.1

dev_dependencies:
  build_runner: ^2.4.8
  flutter_test:
    sdk: flutter
  freezed: ^2.4.7

flutter:
  uses-material-design: false
