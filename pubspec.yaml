name: report_manager
description: "Fuel Reporting Management"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dartz: ^0.10.1
  flutter:
    sdk: flutter
  get_it: ^7.6.7
  gsheets: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
